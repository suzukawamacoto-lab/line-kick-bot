// コマンド追加例
else if (text === '/kick-list') {
  await client.replyMessage(replyToken, { 
    type: 'text', 
    text: '📋 直近のキック履歴:\n- ユーザーA (2026/09/08)\n- ユーザーB (2026/09/07)' 
  });
}
else if (text === '/protect on') {
  await client.replyMessage(replyToken, { type: 'text', text: '🛡️ 蹴り保護をONにしました' });
}
else if (text === '/protect off') {
  await client.replyMessage(replyToken, { type: 'text', text: '⚠️ 蹴り保護をOFFにしました' });
}
else if (text === '/group-id') {
  await client.replyMessage(replyToken, { type: 'text', text: `🆔 グループID: ${groupId}` });
}
